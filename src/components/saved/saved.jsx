import { Container, Typography } from "@mui/material";
import React from "react";

function Saved() {
    return (
        <Container fixed>
            <Typography>Siz hali ham birorta kitob olmadingiz</Typography>
        </Container>
    );
}

export default Saved;
